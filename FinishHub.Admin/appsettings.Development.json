{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Local dev points at the API's IIS Express endpoint (see test/Properties/launchSettings.json).
  // Switch to https://localhost:57370/ when running the API under the Kestrel "test" profile.
  "ApiSettings": {
    "BaseUrl": "https://localhost:44358/",
    // JWT used for the API calls that require authentication (WhatsApp number add/update).
    "AccessToken": ""
  }
}
