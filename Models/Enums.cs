namespace test.Models;

public enum UserType
{
    User = 1,
    Engineer = 2
}

public enum PropertyType
{
    Apartment,
    Villa,
    Office,
    Commercial,
    Other
}

public enum MediaType
{
    Image,
    Video
}
